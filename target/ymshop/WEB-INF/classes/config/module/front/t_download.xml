<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.download">	<select id="front.download.selectPageList" parameterType="net.yozo.services.front.download.bean.Download" resultType="net.yozo.services.front.download.bean.Download">	 select * from t_download t where 1=1	 <if test="account!=null and account!=''"> and account=#{account} </if>  order by lastDownloadTime desc limit #{offset},#{pageSize} 	</select> 	<select id="front.download.selectPageCount" parameterType="net.yozo.services.front.download.bean.Download" resultType="java.lang.Integer">	 select count(*) from t_download where 1=1 <if test="account!=null and account!=''"> and account=#{account} </if>	</select>	<select id="front.download.selectList" parameterType="net.yozo.services.front.download.bean.Download" resultType="net.yozo.services.front.download.bean.Download">		select *		from t_download t where account=#{account} and productID=#{productID}	</select> 	<insert id="front.download.insert" parameterType="net.yozo.services.front.download.bean.Download" useGeneratedKeys="true" keyProperty="id">		insert into t_download		(		id,lastDownloadTime		<if test="account!=null and account!=''">		,account		</if>		<if test="productID!=null and productID!=''">		,productID		</if>		)		values		(		id,now()		<if test="account !=null and account !=''">		,#{account}		</if>		<if test="productID !=null and productID !=''">		,#{productID}		</if>	)</insert>		<update id="front.download.update" parameterType="net.yozo.services.front.download.bean.Download">		update t_download		  set id=#{id}		<if test="account !=null and account !=''">		,account=#{account}		</if>		<if test="productID !=null and productID !=''">		,productID=#{productID}		</if>		 where id=#{id}	</update>		<select id="front.download.selectOne" parameterType="net.yozo.services.front.download.bean.Download" resultType="net.yozo.services.front.download.bean.Download">	 select *	 from t_download where 1=1		<if test="id !=null and id !=''">			and id=#{id}		</if>		<if test="account !=null and account !=''">			and account=#{account}		</if>		<if test="productID !=null and productID !=''">			and productID=#{productID}		</if>	</select>		<select id="front.download.selectCount" parameterType="net.yozo.services.front.download.bean.Download" resultType="int">	 select count(*) from t_download where 1=1	 <if test="account!=null and account!=''"> and account=#{account} </if>	 <if test="productID!=null and productID!=''"> and productID=#{productID} </if>	</select>		<delete id="front.download.deleteById" parameterType="net.yozo.services.front.download.bean.Download">		delete from t_download where id=#{id}	</delete>	<delete id="front.download.delete" parameterType="net.yozo.services.front.download.bean.Download">		delete from t_download where productID=#{productID} and account =#{account}	</delete>	<update id="front.account.updateLastDownloadTime" parameterType="net.yozo.services.front.download.bean.Download">		update t_download set lastDownloadTime=now() where 1=1		<if test="id!=null and id!=''">and id=#{id}</if>		<if test="account!=null and account!=''"> and account=#{account} </if>		<if test="productID!=null and productID!=''"> and productID=#{productID} </if>	</update></mapper>