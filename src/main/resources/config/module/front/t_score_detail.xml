<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.scoreDetail">	<select id="front.scoreDetail.selectPageList" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail" resultType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">	 select * from t_score_detail t where 1=1	 <if test="account!=null and account!=''"> and account=#{account} </if>  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="front.scoreDetail.selectPageCount" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail" resultType="java.lang.Integer">	 select count(*) from t_score_detail where 1=1 <if test="account!=null and account!=''"> and account=#{account} </if>	</select>	<select id="front.scoreDetail.selectList" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail" resultType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">		select *		from t_score_detail t where account=#{account}	</select> 	<insert id="front.scoreDetail.insert" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail" useGeneratedKeys="true" keyProperty="id">		insert into t_score_detail		(		id,date		<if test="account!=null and account!=''">		,account		</if>		<if test="taskid!=null and taskid!=''">		,taskid		</if>		<if test="score!=0">			,score		</if>		<if test="orderid!=null and orderid!=''">			,orderid		</if>		)		values		(		id,now()		<if test="account !=null and account !=''">		,#{account}		</if>		<if test="taskid !=null and taskid !=''">		,#{taskid}		</if>		<if test="score!=0">		,#{score}		</if>		<if test="orderid!=null and orderid!=''">		,#{orderid}		</if>	)</insert>		<update id="front.scoreDetail.update" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">		update t_score_detail		  set id=#{id},date=now()		<if test="account !=null and account !=''">		,account=#{account}		</if>		<if test="taskid !=null and taskid !=''">			,taskid=#{taskid}		</if>		<if test="score !=0">			,score=#{score}		</if>		<if test="orderid!=null and orderid!=''">			,orderid=#{orderid}		</if>		 where id=#{id}	</update>		<select id="front.scoreDetail.selectOne" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail" resultType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">	 select *	 from t_score_detail t where 1=1		<if test="id !=null and id !=''">			and id=#{id}		</if>	    <if test="account !=null and account !=''">			and account=#{account}	    </if>		<if test="taskid !=null and taskid !=''">			and taskid=#{taskid}		</if>		<if test="score !=0">			and score=#{score}		</if>		<if test="orderid!=null and orderid!=''">			and orderid=#{orderid}		</if>	</select>	<delete id="front.scoreDetail.deleteById" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">		delete from t_score_detail where id=#{id}	</delete>	<delete id="front.scoreDetail.delete" parameterType="net.yozo.services.front.scoreDetail.bean.ScoreDetail">		delete from t_score_detail where productID=#{productID} and account =#{account}	</delete></mapper>