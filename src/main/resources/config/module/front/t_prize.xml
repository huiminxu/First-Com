<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.prize"> 	<insert id="front.prize.insert" parameterType="net.yozo.services.common.Prize" useGeneratedKeys="true" keyProperty="id">		insert into t_prize		(		id,getPrizeDate		<if test="account!=null and account!=''">			,account		</if>		<if test="prize!=null and prize!=''">			,prize		</if>		)		values		(		id,now()		<if test="account!=null and account!=''">			,#{account}		</if>		<if test="prize!=null and prize!=''">			,#{prize}		</if>		)</insert>	<select id="front.prize.selectTodayPrize" parameterType="net.yozo.services.common.Prize" resultType="net.yozo.services.common.Prize">		select * from t_prize t where 1=1		<if test="account!=null and account!=''"> and account=#{account} </if>		<if test="getPrizeDate!=null and getPrizeDate!=''"> and getPrizeDate>=#{getPrizeDate} </if>	</select>	<select id="front.prize.selectList" parameterType="net.yozo.services.common.Prize" resultType="net.yozo.services.common.Prize">		select * from t_prize order by getPrizeDate desc limit 10	</select></mapper>