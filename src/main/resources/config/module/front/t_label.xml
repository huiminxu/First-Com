<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.label">	<select id="front.label.selectPageList" parameterType="net.yozo.services.front.label.bean.Label" resultType="net.yozo.services.front.label.bean.Label">	 select t.*,(select c.name from t_catalog c where c.id=t.catalogID) catalogName 	 from t_label t where pid=#{pid}	 <if test="catalogID!=0">	 	and catalogID=#{catalogID} 	 </if> order by order1 limit #{offset},#{pageSize} 	</select> 	<select id="front.label.selectPageCount" parameterType="net.yozo.services.front.label.bean.Label" resultType="java.lang.Integer">	 select count(*) from t_label t where pid=#{pid}	 <if test="catalogID!=0">	 	and catalogID=#{catalogID} 	 </if>	</select> 	<insert id="front.label.insert" parameterType="net.yozo.services.front.label.bean.Label" useGeneratedKeys="true" keyProperty="id">		insert into t_label		(id,catalogID,order1,name,pid)		values		(id,#{catalogID},#{order1},#{name},#{pid})	</insert>		<select id="front.label.selectList" parameterType="net.yozo.services.front.label.bean.Label" resultType="net.yozo.services.front.label.bean.Label">	 select * from t_label t where pid=#{pid}	 <if test="id!=null and id!=''">	 	and id=#{id}	 </if>	 <if test="catalogID!=0">	 	and catalogID=#{catalogID}	 </if>	  order by order1  	</select>	<select id="front.label.selectChidrenIdsByPid" parameterType="int" resultType="int">		select id from t_label t where pid=#{pid}			order by order1	</select>		<update id="front.label.update" parameterType="net.yozo.services.front.label.bean.Label">		update t_label		  set id=#{id},order1=#{order1}		<if test="name !=null and name !=''">		,name=#{name}		</if>		<if test="catalogID !=0">		,catalogID=#{catalogID}		</if>		<if test="pid !=0">		,pid=#{pid}		</if>		 where id=#{id}	</update>	<select id="front.label.selectOne" parameterType="net.yozo.services.front.label.bean.Label" resultType="net.yozo.services.front.label.bean.Label">	 select * 	 from t_label t where 1=1  	 <if test="id!=null and id!=''">	 	and id=#{id}	 </if>	 <if test="pid!=0">	 	and pid=#{pid}	 </if>	 <if test="catalogID!=0">	 	and catalogID=#{catalogID}	 </if>	</select>		<select id="front.label.selectById" parameterType="string" resultType="net.yozo.services.front.label.bean.Label">	 select * from t_label t where id=#{id}	</select>		<delete id="front.label.deleteById" parameterType="int">		delete from t_label where id=#{id}	</delete>		<delete id="front.label.deleteByPid" parameterType="int">		delete from t_label where  pid=#{pid}	</delete>		<select id="front.label.selectCount" parameterType="net.yozo.services.front.label.bean.Label" resultType="int">		select count(*) from t_label where 1=1 		 <if test="pid!=0">	 	  and pid=#{pid}		 </if>		 <if test="catalogID!=0">		 	and catalogID=#{catalogID}		 </if>	</select>	<!-- 	<delete id="front.label.deleteAllById" parameterType="int"> --><!-- 		BEGIN delete from t_label_link where attrID in (SELECT id FROM t_label where pid=#{id}); --><!-- 		delete from t_label where 1=1 and pid=#{id}; --><!-- 		delete from t_label where 1=1 and id=#{id}; --><!-- 		end; --><!-- 	</delete> -->		</mapper>