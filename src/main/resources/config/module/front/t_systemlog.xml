<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="front.systemlog">	<select id="front.systemlog.selectPageList" parameterType="net.yozo.services.front.systemlog.bean.Systemlog" resultType="net.yozo.services.front.systemlog.bean.Systemlog">	 select 		t.id "id",		t.title "title",		t.content "content",		t.type "type",		t.createTime "createTime"	 from t_systemlog t order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="front.systemlog.selectPageCount" parameterType="net.yozo.services.front.systemlog.bean.Systemlog" resultType="java.lang.Integer">	 select count(*) from t_systemlog	</select> 	<insert id="front.systemlog.insert" parameterType="net.yozo.services.front.systemlog.bean.Systemlog" useGeneratedKeys="true" keyProperty="id">		insert into t_systemlog		(		id,createTime		<if test="title!=null and title!=''">		,title		</if>		<if test="content!=null and content!=''">		,content		</if>		<if test="type!=0">		,type		</if>		)		values		(		id,now()		<if test="title !=null and title !=''">		,#{title}		</if>		<if test="content !=null and content !=''">		,#{content}		</if>		<if test="type !=0">		,#{type}		</if>	)</insert>	<update id="front.systemlog.update" parameterType="net.yozo.services.front.systemlog.bean.Systemlog">		update t_systemlog		  set id=#{id}		<if test="title !=null and title !=''">		,title=#{title}		</if>		<if test="content !=null and content !=''">		,content=#{content}		</if>		<if test="type !=0">		,type=#{type}		</if>		 where id=#{id}	</update>	<select id="front.systemlog.selectOne" parameterType="net.yozo.services.front.systemlog.bean.Systemlog" resultType="net.yozo.services.front.systemlog.bean.Systemlog">	 select 		t.id "id",		t.title "title",		t.content "content",		t.type "type",		t.createTime "createTime"	 from t_systemlog t where id=#{id}	</select>	<delete id="front.systemlog.deleteById" parameterType="net.yozo.services.front.systemlog.bean.Systemlog">		delete from t_systemlog where id=#{id}	</delete></mapper>