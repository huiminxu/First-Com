package net.yozo.services.front.favorite.impl;import net.yozo.core.ServersManager;import net.yozo.core.dao.page.PagerModel;import net.yozo.services.front.TopicTemplate.bean.TopicTemplate;import net.yozo.services.front.favorite.FavoriteService;import net.yozo.services.front.favorite.bean.Favorite;import net.yozo.services.front.favorite.dao.FavoriteDao;import net.yozo.services.front.topic.bean.Topic;import net.yozo.web.util.RequestHolder;import org.springframework.stereotype.Service;import javax.annotation.Resource;import java.util.List;@Service("favoriteServiceFront")public class FavoriteServiceImpl extends ServersManager<Favorite, FavoriteDao> implements		FavoriteService {    @Resource(name = "favoriteDaoFront")    @Override    public void setDao(FavoriteDao favoriteDao) {        this.dao = favoriteDao;    }    @Override	public int selectCount(Favorite favorite) {		return dao.selectCount(favorite);	}    /**     * 查询收藏topic     * @param fovorite     * @return     */    public PagerModel selectTopicByAccount(Favorite fovorite){        int offset = 0;        if (RequestHolder.getRequest().getParameter("pager.offset") != null) {            offset = Integer                    .parseInt(RequestHolder.getRequest().getParameter("pager.offset"));        }        if (offset < 0)            offset = 0;        PagerModel pager = new PagerModel();        pager = dao.selectByAccount(fovorite);        if(pager==null)pager = new PagerModel();        // 计算总页数        pager.setPagerSize((pager.getTotal() + pager.getPageSize() - 1)                / pager.getPageSize());        pager.setOffset(offset);        return pager;    }    /**     * 查询收藏topic数量     * @param favorite     * @return     */    public int selectTopicCount(Favorite favorite){        return dao.selectTopicCount(favorite);    }}