package net.yozo.services.front.scoreDetail;import net.yozo.core.Services;import net.yozo.services.front.order.bean.Order;import net.yozo.services.front.scoreDetail.bean.ScoreDetail;public interface ScoreDetailService extends Services<ScoreDetail> {    /**     * 根据积分任务的flag,对积分相关内容完成全部的操作     * 操作     *@param accid 用户id     *@param flag  积分任务标识     */    void addScoreAllOperations(String accid,String flag );    /**     * 根据积分任务的flag,对积分相关内容完成全部的操作（用户总积分变更、增加积分明细、增加成长值、积分任务）     *@param order 积分订单信息     */    String reduceScoreAllOperations(Order order);}