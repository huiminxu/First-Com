package net.yozo.services.front.address.impl;import net.yozo.core.ServersManager;import net.yozo.services.front.address.AddressService;import net.yozo.services.front.address.bean.Address;import net.yozo.services.front.address.dao.AddressDao;import org.springframework.stereotype.Service;import javax.annotation.Resource;@Service("addressServiceFront")public class AddressServiceImpl extends ServersManager<Address, AddressDao> implements		AddressService {    @Resource(name = "addressDaoFront")    @Override    public void setDao(AddressDao addressDao) {        this.dao = addressDao;    }    @Override	public void setAddressDefault(Address address) {		dao.initAllAddress(address);        dao.update(address);//设置指定的地址为默认地址	}}